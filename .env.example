# Postgres connection string — a free Rivestack database works out of the box:
# https://rivestack.io (Dashboard -> your database -> Connection string)
DATABASE_URL=

# Used twice: text-embedding-3-small for embeddings, gpt-4o-mini for answers.
OPENAI_API_KEY=
